# apps/web 功能需求文档

## 项目概述
企业知识库工作台前端（Next.js 16 + React 19 + Tailwind v4）。面向知识库治理的 Web 应用，提供文档批量入库、人工复核、文档库管理、检索验证、知识库集合管理等功能。

---

## 一、全局功能

### 1.1 应用外壳（AppShell）
- **左侧导航栏**
  - Logo + 产品名称 "Knowledge Workbench"
  - 导航项：批量入库、人工复核、文档库、检索验证、知识库集合
  - 当前页面高亮（背景色 + 文字色 + 右侧脉冲圆点）
  - 移动端：汉堡菜单触发抽屉式侧边栏，遮罩层关闭
  - 桌面端：可折叠/展开侧边栏（带动画 width 过渡）
- **顶部 Header**
  - 侧边栏切换按钮（汉堡/X 图标动画切换）
  - 后端服务健康状态指示灯（Admin / Workbench / Access / Retrieval / Ingestion）
  - "All Healthy" 状态徽标（绿点脉冲动画）
  - 知识库集合选择器（下拉选择当前操作的集合，从 /workbench/me 获取租户）
  - 设置入口按钮
- **内容区**
  - 页面切换时淡入 + 上移动画
  - 最大宽度 max-w-7xl，居中布局
- **底部 Footer（侧边栏底部）**
  - "Retrieval workbench" / "Non-answer-generation agent"

### 1.2 全局状态管理（Zustand）
- `currentCollectionId`: 当前选中的知识库集合 ID
- `demoToken`: 演示 JWT 令牌
- `demoApiKey`: 演示 API 密钥（访问服务用）
- `accessScope`: 权限范围（内部/外部，含部门、角色、用户、组、代理类型、客户、应用等字段）

### 1.3 全局交互
- **Toast 通知**：操作成功/失败提示（sonner）
- **错误边界**：捕获渲染错误，展示友好错误页
- **TooltipProvider**：全局 Tooltip 上下文
- **动画系统**：staggerContainer / staggerItem / overlayFade / slideInFromLeft 等预设动画
- **后端缺口（BackendGap）提示**：当 API 返回 501/未实现时，展示功能尚未就绪的提示卡片

### 1.4 API 客户端
- Workbench API 封装：支持超时控制、JWT Cookie 同步、401 自动登出
- 错误处理：区分 ApiClientError / BackendGapError / 网络错误
- 健康检查：/health/all 轮询（30s 间隔）

---

## 二、首页（/）

### 2.1 功能
- 服务端重定向到 `/upload`

---

## 三、批量入库页（/upload）

### 3.1 上传区
- **拖拽上传**：支持拖拽文件到上传区（高亮边框 + 渐变背景动画）
- **点击上传**：点击区域唤起文件选择器，支持多选
- **支持格式**：PDF、DOCX、PPTX、XLSX、CSV
- **前置校验**：未选择集合或未配置权限范围时禁止上传，Toast 提示错误
- **并发控制**：最大 3 个并发上传，其余排队

### 3.2 文件状态追踪
- 每个文件卡片展示：文件类型图标、文件名、文件大小、状态徽标
- **状态流转**：排队 → 上传中 → 已上传 → 解析中 → 复核中 → 已批准/已发布 → 索引构建中
- 失败状态：展示错误信息，提供重试按钮
- 上传中/解析中/索引构建中：底部显示进度条（shimmer 动画）
- 卡片左侧状态色条（根据状态变色）
- 删除按钮：从列表移除

### 3.3 统计面板
- 当前批次统计卡片（5 个指标）：总数、处理中、已入库、待复核、失败
- 仅在存在文件时展示

### 3.4 最近任务列表
- 无限滚动加载（IntersectionObserver）
- 每页 20 条，按上传时间倒序
- 展示：文件名、集合 ID、状态徽标
- 加载更多指示器、总记录数提示
- 当前文件与任务状态联动：通过任务 API 轮询（5s/30s）更新文件状态

---

## 四、人工复核队列页（/review）

### 4.1 工单列表
- 展示所有复核工单（Ticket），按更新时间倒序
- 每条工单卡片展示：
  - 左侧状态色条
  - 状态图标（待复核/已批准/已拒绝/已退回）
  - 文件名（回退到标题/doc_id/ticket_id）
  - 集合 ID Badge、工单 ID（monospace）、相对时间（刚刚/N分钟前/N天前）
  - 状态徽标（待复核带脉冲动画）
  - 右侧 Chevron 导航指示
- 点击卡片进入工单详情页 `/review/[taskId]`

### 4.2 筛选器
- **集合筛选**：下拉选择全部或特定集合
- **状态筛选**：全部、待复核、已批准、已拒绝、已退回
- 清除筛选按钮（仅在筛选非默认时展示）

### 4.3 空状态
- 无工单时展示 EmptyState 插图 + 描述

---

## 五、复核详情页（/review/[taskId] & /workbench/[ticketId]）

### 5.1 页面头部（Hero Section）
- 返回按钮（链接到复核队列）
- 状态徽标组：工单状态、失败码、下一步动作、文档关联来源
- 文档标题（回退链：文件名 → doc_id → ticket_id）
- 描述文案
- **四宫格指标卡片**：Findings 数、Risks（Critical/High）数、Draft edits 数、Warnings 数
- **元数据网格**：工单 ID、集合 ID、文档 ID、解析快照 ID

### 5.2 数据源降级提示
- 当 workspace 部分数据不可用时，展示 Alert 提示 degraded_parts

### 5.3 主内容区（三 Tabs）

#### Tab 1: Source（源文件预览）
- 文档查看器组件：
  - 支持 PDF（Canvas 渲染，分页 + 缩放）、图片、HTML（iframe）、纯文本
  - 工具栏：缩放（50%-200%）、复制解析文本、下载源文件
  - 分页导航：上一页/下一页、页码输入跳转
  - 锚定页按钮：根据 chunks 的 page_spans 生成快捷跳转按钮
  - 双 Tab：Source（原始文件）/ Parsed text（解析后文本）
  - 文本搜索高亮：支持从 Agent Review 点击 "Find in document" 自动搜索并高亮定位
  - 无源文件时展示 Alert 提示
- 解析上下文卡片：展示源文件名、后缀、解析器 ID、后端、生效策略
- 预览信号卡片：Chunks 数、Warnings 数、Edits 数、预览文本（max-h-52 滚动区域）

#### Tab 2: Draft edits（草稿编辑）
- 仅在有解析快照时可用
- ChunkEditorWorkbench 组件：
  - 搜索 chunks（按内容或 evidence_id）
  - 展示 chunk 列表：evidence_id Badge、section_path、页码范围
  - 展开/折叠长内容（>300 字符）
  - 编辑按钮：打开 ChunkEditModal
  - 高亮定位：从 Agent Review 点击 "Edit chunk" 自动滚动到对应 chunk
- 预发布模式：编辑提交到草稿层，不直接修改检索索引

#### Tab 3: Agent review（代理审核）
- AgentReviewPanel 组件：
  - 按严重程度排序（Critical → High → Medium → Low → Info）
  - 每条 Finding 卡片：严重程度 Badge、分类 Badge、置信度、问题摘要
  - 展开后展示：Source quote（引用块）、"Find in document" 按钮、"Edit chunk" 按钮、页码范围

### 5.4 右侧边栏（Decision Cockpit + Ticket Context + System & Diagnostics）

#### Decision Cockpit
- 当前状态展示（Badge 组）
- 若工单处于待复核状态且有权限：
  - 决策原因文本框（可选）
  - 三个操作按钮：Approve（通过）、Reject（驳回）、Return for revision（退回修改）
- 若已有决策：展示决策原因（或 "No additional reason"）

#### Ticket Context
- 工单 ID、集合 ID、文档 ID、解析快照 ID

#### System and Diagnostics
- 系统决策标签（System auto-approved/rejected/returned）
- 决策者、决策原因
- 失败码、失败阶段、下一步动作、降级部件

### 5.5 服务端聚合
- 页面数据通过 `workbenchApi.getWorkspaceDetail(ticketId)` 一次性获取聚合视图
- 包含：ticket、document、parse_snapshot、chunks、chunk_edits、agent_review、capabilities、degraded_parts

---

## 六、文档库页（/documents）

### 6.1 文档列表
- 列表展示所有文档投影（DocumentProjection），按 projection_updated_at 倒序，最多 200 条
- 每条文档卡片展示：
  - 复选框（管理员可批量选择）
  - 文件类型图标（PDF/DOC/PPT/Sheet，按扩展名匹配不同颜色）
  - 文件名（可点击跳转 `/documents/[docId]`）
  - Review 状态 Badge、Indexed Badge
  - 集合 ID、chunk 数、页数、完整度（Complete/Partial）、相对更新时间
  - 降级原因（如有）
  - 文档状态徽标（Active/Pending/Archived）
  - Stale 标记（红色 destructive Badge）
  - 右侧 Chevron 跳转按钮

### 6.2 筛选器（7 个）
- 关键词搜索（文件名或 doc_id 模糊匹配）
- 集合筛选
- 状态筛选（Active/Pending/Archived）
- 复核状态筛选（Pending/Approved/Rejected）
- 文件类型筛选（PDF/Document/Presentation/Spreadsheet）
- 新鲜度筛选（Stale only / Fresh only）
- 索引状态筛选（Indexed / Not indexed）
- 可见文档计数展示

### 6.3 批量操作（仅限管理员）
- 选择文档后展示批量操作栏：Archive（归档）、Retract（撤回）、Reindex（重建索引）
- 全选/取消全选当前可见文档
- 操作确认弹窗：输入原因、Reindex 需输入 index_profile_id
- 批量操作结果展示：总数量、成功数、失败数、每条结果的详细状态

### 6.4 管理员标识
- 拥有 knowledge_admin 或 platform_admin 角色的用户，展示 "Admin lifecycle enabled" Badge

---

## 七、文档详情页（/documents/[docId]）

### 7.1 页面结构
- 与复核详情页类似，但面向已发布文档的生命周期管理
- 头部展示：文档状态、Review 状态、Task 状态、STALE 标记、降级部件标记
- **四宫格指标**：Findings、Blocking（Critical/High）、Chunks、Draft edits
- **元数据网格**：Doc ID、Collection、Source File、Parse Snapshot

### 7.2 主内容区（三 Tabs）
- Source：同复核详情页的 DocumentViewer
- Chunks：同复核详情页的 ChunkEditorWorkbench（post-publish 模式）
- Agent：同复核详情页的 AgentReviewPanel

### 7.3 右侧边栏

#### Review Cockpit
- 若文档关联了复核工单：展示工单状态、决策、决策原因
- 待复核状态 + 有权限时：提供 Approve/Reject/Return 按钮 + 原因输入

#### Lifecycle Actions
- **Archive Document**：归档文档（需有 can_archive 权限）
- **Retract Document**：撤回文档（destructive 样式，需有 can_retract 权限）
- **Reindex Document**：重建索引（需有 can_reindex 权限）
- 无权限时展示提示文案

#### Diagnostics
- Task Status、Index Version、Next Action、Failure Stage
- 失败码 Alert 展示

### 7.4 生命周期操作弹窗
- 输入操作原因
- Reindex 需输入 index_profile_id
- Confirm / Cancel

---

## 八、检索验证页（/retrieval）

### 8.1 检索表单
- **查询输入框**：标准检索字段，Enter 触发检索
- **Token 预算输入**：数字输入，默认 2000
- **集合选择器**：下拉选择知识库集合（从 workbench 获取）
- **检索配置选择器**：下拉选择已发布的检索配置（retrieval profile）
- **调试级别选择**：无 / 基础 / 完整（Tooltip 解释各级含义）
- **检索按钮**：未选择集合或配置时禁用
- 未选集合/配置时展示 Alert 提示

### 8.2 检索结果
- 证据片段列表（evidence_items）
- 结果头部：标题 + 展开全部/收起全部按钮 + 计数 Badge（X 项 · 已用 Y Token）
- 每条证据卡片：
  - 序号 Badge（渐变背景）
  - 相关性分数（Score）+ 可视化进度条
  - 复制内容按钮（带 1.5s 成功状态反馈）
  - 展开/折叠按钮
  - 元数据行：doc_id、evidence_id、collection_id
  - 内容区（默认折叠 4 行）
  - 展开后展示：source_stage、why_selected、section_path

### 8.3 空结果
- 展示 EmptyState："无证据片段" + 提示检查查询/集合/配置

---

## 九、知识库集合页（/collections）

### 9.1 集合列表
- 卡片网格布局（1-3 列响应式）
- 每张卡片展示：
  - 文件夹图标 + 集合名称
  - 生命周期状态 Badge（active/其他）
  - 集合 ID（monospace，小号字）
  - 描述（2 行截断，无描述显示 "无描述"）
  - 租户 ID
  - "选择用于上传" 按钮：点击后设置当前集合并 Toast 成功提示
- 卡片悬停渐变覆盖效果

### 9.2 新建集合
- 弹窗表单：集合 ID、名称、描述
- 提交按钮：依赖租户 ID（从 /workbench/me 获取）
- 创建成功后：刷新列表、关闭弹窗、重置表单、Toast 成功
- 后端缺口时展示 BackendGap

### 9.3 加载与空状态
- 加载中：3 个骨架屏卡片
- 错误：Alert 展示错误信息
- 无集合：EmptyState + 创建按钮

---

## 十、设置页（/settings）

### 10.1 自定义 Tabs 切换
- 认证 Tab / 权限范围 Tab
- 动画：layoutId 弹簧动画的活跃指示器

### 10.2 认证设置
- **演示 JWT 令牌**：password 类型输入框，用于工作台/管理 API
- **演示 API 密钥**：password 类型输入框，用于访问服务（X-API-Key）
- 保存按钮：更新 Zustand store，Toast 成功

### 10.3 权限范围设置
- **范围类型切换**：内部（Internal）/ 外部（External），Segmented Control 样式
- 内部模式字段：部门、角色、用户、组
- 外部模式字段：代理类型 ID、API 密钥、客户、应用
- 保存按钮：组装 scope 对象存入 Zustand，Toast 成功

---

## 十一、通用 UI 组件需求

### 11.1 Card
- 支持 `interactive` 模式：悬停时 cursor-pointer + 轻微位移阴影
- 支持圆角变体（rounded-xl / rounded-2xl / rounded-[24px] / rounded-[28px]）
- glass-card / glass-strong 变体（半透明背景 + 边框）

### 11.2 Badge
- 变体：default、secondary、destructive、outline、success
- 支持小号（text-[10px] h-5）和常规尺寸

### 11.3 Dialog
- 圆角 glass 风格弹窗
- 支持标准头部/内容/底部结构

### 11.4 Select
- glass 风格触发器（圆角 full / rounded-xl）
- 下拉内容带阴影和圆角

### 11.5 Skeleton
- 圆角矩形占位，animate-pulse

### 11.6 EmptyState
- 图标 + 标题 + 描述 + 可选操作按钮

### 11.7 Alert
- 变体：默认 / destructive（红色主题）
- 支持图标 + 描述内容

---

## 十二、技术约束

- **框架**：Next.js 16（App Router），React 19
- **样式**：Tailwind CSS v4，CSS 变量主题系统
- **字体**：Geist Sans + Geist Mono（Google Fonts）
- **状态管理**：Zustand（客户端全局状态）
- **数据获取**：TanStack Query v5（React Query）
- **动画**：Framer Motion（页面过渡、列表 stagger、tab 切换、侧边栏展开）
- **通知**：Sonner（Toast）
- **图标**：Lucide React
- **PDF 预览**：pdfjs-dist（动态导入 webpack.mjs 入口）
- **服务端**：无 SSR 数据获取（所有数据在客户端通过 React Query 获取），页面标记 "use client"

---

## 十三、已实现功能模块（原待办列表，全部已完成 ✅）

> 以下功能原为产品经理视角梳理的待办需求，现已全部完成开发并上线。

### 13.1 ✅ 数据仪表盘（Dashboard）
- **路由**：`/`（首页）
- **组件**：`dashboard-page.tsx`
- **功能**：
  - 时区感知问候语（早上好/下午好/晚上好）
  - 4 个统计卡片（今日上传量、待复核数、文档总数、Stale 比例）
  - 6 个快捷操作卡片（上传、复核、文档库、检索、集合、设置）
  - 最近工单列表（点击跳转复核详情）
  - 公告/运营 Banner
  - 加载骨架屏、错误 Alert、空状态处理

### 13.2 ✅ 通知中心
- **路由**：全局 Header 右侧铃铛图标
- **组件**：`notification-center.tsx`
- **功能**：
  - 未读数徽标（Badge）
  - 下拉面板（scale + fade 动画）
  - 通知列表（标题、消息、时间戳）
  - 已读/未读视觉区分（不透明度、蓝点指示）
  - 点击标记已读并跳转链接
  - "全部标记已读"按钮
  - 加载骨架屏、错误 AlertTriangle、空状态

### 13.3 ✅ 检索配置管理（Retrieval Profile Admin）
- **路由**：`/retrieval/profiles`
- **组件**：`retrieval-profiles-page.tsx`
- **功能**：
  - 列表展示所有检索配置（名称、描述、发布/草稿 Badge）
  - 配置字段：rerank_model、top_k、similarity_threshold、token_budget_limit
  - 完整 CRUD + 克隆 + 发布操作
  - 加载骨架屏、错误 Alert、空状态+创建引导

### 13.4 ✅ 解析策略管理（Parse Policy Admin）
- **路由**：`/settings/parser-profiles`
- **组件**：`parser-profiles-page.tsx`
- **功能**：
  - 列表展示所有解析策略（名称、描述、发布/草稿 Badge）
  - 配置字段：parser_id、OCR、表格检测、语言
  - 完整 CRUD + 克隆 + 发布操作
  - 加载骨架屏、错误 Alert、空状态+创建引导

### 13.5 ✅ 审计日志（Audit Log）
- **路由**：`/settings/audit-log`
- **组件**：`audit-log-page.tsx`
- **功能**：
  - 筛选器：操作类型（9 种）、日期范围、文档 ID、集合 ID、操作人 ID
  - 分页表格：日志 ID、操作人邮箱、操作类型（彩色 Badge）、目标类型/ID、时间戳、IP
  - 页大小选择（10/20/50/100）、分页导航
  - CSV / Excel 导出
  - 加载骨架屏、错误 Alert、空状态

### 13.6 ✅ 工单协作与评论
- **组件**：`ticket-comments.tsx`（集成在 `ticket-detail.tsx`）
- **功能**：
  - 创建新评论（文本域 + 发送按钮）
  - 编辑/删除自己的评论（Hover 可见操作按钮）
  - @提及 高亮（`@username` 渲染为主色）
  - 相对时间戳（中文：刚刚、X分钟前等）
  - 作者展示（名称/邮箱/ID 回退链）
  - 空状态（"写下第一条评论吧"）
  - **指派机制**：工单列表已支持"我的待办"/"全部"/"未分配"筛选
  - **处理记录时间轴**：工单详情页已有完整生命周期时间轴

### 13.7 ✅ API 密钥与访问管理
- **路由**：`/settings/api-keys`
- **组件**：`api-keys-page.tsx`
- **功能**：
  - 密钥列表（名称、前缀、状态 Badge、权限标签、过期时间、最后使用时间）
  - 完整 CRUD（创建权限复选框、编辑、吊销确认弹窗）
  - 创建后展示完整密钥 + 一键复制
  - 用量统计弹窗：总请求数、总 Token、峰值 QPS、每日统计表
  - 密钥详情弹窗
  - 加载骨架屏、错误 Alert、空状态+创建引导

### 13.8 ✅ 回收站（Trash）
- **路由**：`/trash`
- **组件**：`trash/page.tsx`
- **功能**：
  - 已删除文档列表（文件类型图标、源集合、删除时间、删除人）
  - 搜索（按文件名/doc_id/集合 ID）
  - 排序（删除时间/文件名/自动清理日）
  - 单个 & 批量恢复（确认弹窗）
  - 单个 & 批量彻底删除（警告弹窗，destructive 样式）
  - 30 天自动清理倒计时（≤7 天显示红色 Badge）
  - 侧边栏导航入口

### 13.9 ✅ 帮助中心与 Onboarding
- **路由**：`/help`
- **组件**：`help/page.tsx`、`onboarding-tour.tsx`
- **功能**：
  - 帮助中心：5 个 FAQ 带标签过滤、关键词搜索（匹配题目/答案/标签）
  - 快捷键参考表（Cmd+K、/、?、Esc）
  - 功能导航卡片（上传指南、复核指南）
  - Onboarding Tour：4 步 Spotlight 引导（上传、复核、检索、设置）
  - 进度条、上一步/下一步/跳过控制
  - 首次访问检测（localStorage `ekb-onboarding-completed`）
  - 帮助图标在 AppShell Header

### 13.10 ✅ 全局搜索（Command Palette）
- **组件**：`command-palette.tsx`（全局集成在 AppShell）
- **功能**：
  - 触发：Cmd/Ctrl+K 或点击 Header 搜索框
  - 搜索范围：页面路由（7 个）、文档、工单、集合
  - 键盘导航：上下箭头选择、Enter 跳转、Esc/点击外部关闭
  - 结果类型标签（页面/文档/工单/集合）
  - 加载骨架屏、暗色半透明遮罩

---

## 十四、现有页面功能缺口分析（已实现/待实现状态标注）

> 逐页审视已开发页面，从操作效率、信息完整性、协作能力、容错兜底四个维度梳理功能缺口。以下标注每个缺口当前实现状态。

### 14.1 首页（/）— 全部缺口已补齐 ✅

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 数据仪表盘入口 | ✅ 已实现 | 首页已重构为 Dashboard |
| 快捷操作区 | ✅ 已实现 | 6 个快捷操作卡片 |
| 系统公告/运营 Banner | ✅ 已实现 | 公告 Banner 组件 |

### 14.2 批量入库页（/upload）— 大部分已补齐

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 文件搜索过滤 | ✅ 已实现 | 按文件名/状态搜索 |
| 排序能力 | ✅ 已实现 | SortDropdown 支持多维度排序 |
| 批量操作 | ✅ 已实现 | 批量删除 + 批量重试 |
| 任务取消 | ✅ 已实现 | 调用 API 取消上传中/排队的任务 |
| 上传速度/ETA | ✅ 已实现 | 实时速度 + 预计剩余时间 |
| 文件大小预警 | ✅ 已实现 | >50MB 前置提示 |
| 上传模板下载 | ✅ 已实现 | CSV 模板文件 + Tooltip 按钮 |
| 断点续传/分片上传 | ❌ 未实现 | |
| 上传历史持久化 | ✅ 已实现 | localStorage 保持批次状态防刷新丢失 |

### 14.3 人工复核队列页（/review）— 全部缺口已补齐 ✅

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 搜索能力 | ✅ 已实现 | 按工单 ID/文件名/文档 ID 搜索 |
| 排序能力 | ✅ 已实现 | SortDropdown 多维度排序 |
| 分页 | ✅ 已实现 | 分页导航 |
| 优先级标记 | ✅ 已实现 | P0/P1/P2 Badge 样式 |
| 分配人维度 | ✅ 已实现 | 全部/我的待办/未分配 筛选 |
| 批量审批 | ✅ 已实现 | 批量批准 + 批量拒绝 |
| 队列统计 | ✅ 已实现 | 队列统计栏 |
| 卡片快捷预览 | ✅ 已实现 | Hover Popover 展示 severity 摘要 |
| 自动刷新开关 | ✅ 已实现 | 5s 自动轮询开关 |
| 空队列占位 | ✅ 已实现 | "恭喜，队列已清空" 正向反馈 |

### 14.4 复核详情页（/review/[taskId] & /workbench/[ticketId]）— 部分已补齐

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 工单评论/讨论区 | ✅ 已实现 | TicketComments 组件 |
| 工单转让 | ✅ 已实现 | 转让弹窗 + API |
| 完整时间轴 | ✅ 已实现 | 工单生命周期时间轴已存在 |
| 源文件 vs 解析结果 并排对比 | ✅ 已实现 | 对比 Tab（Source / Parsed text 并列展示） |
| 审核报告导出 | ✅ 已实现 | 导出按钮 + JSON 格式报告 |
| 审核意见模板 | ✅ 已实现 | 决策原因下拉模板 |
| 相似工单推荐 | ❌ 未实现 | |
| 审核计时器 | ✅ 已实现 | ReviewTimer 组件（实时计时 + 严重等级） |
| 并发编辑冲突提示 | ✅ 已实现 | ConcurrentEditBanner 组件 |
| 工作台差异化 | ✅ 已实现 | 预发布/已发布编辑模式双向区分 |

### 14.5 文档库页（/documents）— 大部分已补齐

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 排序能力 | ✅ 已实现 | 多维度排序 |
| 视图切换 | ✅ 已实现 | list/card/grid 三种视图 |
| 快速预览 | ✅ 已实现 | DocumentPreviewDialog 浮层 |
| 文档标签系统 | ❌ 未实现 | |
| 收藏/置顶 | ✅ 已实现 | 收藏文档，列表顶部展示 |
| 最近访问 | ✅ 已实现 | 最近查看的文档快捷入口 |
| 文档质量评分 | ❌ 未实现 | |
| 检索热度 | ❌ 未实现 | |
| 文档关联图谱 | ❌ 未实现 | |
| 列表项操作菜单 | ✅ 已实现 | DocumentActionMenu 组件 |

### 14.6 文档详情页（/documents/[docId]）— 部分已补齐

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 文档批注/评论 | ✅ 已实现 | DocumentAnnotations 组件 |
| 版本历史 | ⏳ 侧边栏已存在，版本对比待完善 | 失败（Partially Implemented） |
| 分享链接 | ✅ 已实现 | 分享弹窗 |
| 访问记录 | ⏳ 侧边栏已存在，展示占位数据 | 失败（Partially Implemented） |
| 关联文档 | ✅ 已实现 | 相关文档推荐区 |
| 导出功能 | ✅ 已实现 | JSON 导出 |
| 打印优化 | ✅ 已实现 | `globals.css` 中 `@media print` 样式 |
| 更多预览格式 | ❌ 未实现 | 仍限 PDF/图片/HTML/文本 |
| 全文搜索高亮增强 | ❌ 未实现 | |

### 14.7 检索验证页（/retrieval）— 大部分已补齐

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 检索历史 | ✅ 已实现 | localStorage 保存，一键回填查询 |
| 结果对比 | ✅ 已实现 | 两套配置并行检索 + 对比展示 |
| 结果评分反馈 | ✅ 已实现 | 👍/👎 按钮 |
| 结果导出 | ✅ 已实现 | JSON / Markdown 导出 |
| 检索耗时展示 | ✅ 已实现 | 查询延迟显示 |
| 空查询提示 | ✅ 已实现 | 热门查询 + 最近查询 + 示例查询 |
| 检索配置管理入口 | ✅ 已实现 | 通过 `/retrieval/profiles` 管理 |
| 高级检索模式 | ✅ 已实现 | 布尔表达式 + doc_id 范围 + 日期范围 |
| 检索结果可视化 | ❌ 未实现 | 知识图谱展示 |
| API 代码片段 | ✅ 已实现 | cURL / Python SDK 弹窗 |
| 检索参数预设 | ✅ 已实现 | localStorage 保存 + 一键回填 |

### 14.8 知识库集合页（/collections）— 部分已补齐

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 集合详情页 | ✅ 已实现 | `[collectionId]` 子页面 |
| 集合统计 | ✅ 已实现 | 文档数、Chunks/Pages、最后上传、平均 Chunk 数 |
| 集合编辑 | ✅ 已实现 | 编辑名称/描述弹窗 |
| 集合删除 | ✅ 已实现 | 删除确认弹窗 + API |
| 集合搜索 | ✅ 已实现 | 按名称搜索（主列表+详情页） |
| 集合排序 | ✅ 已实现 | 按创建时间/名称/更新时间排序 |
| 集合权限配置 | ✅ 已实现 | 权限查看弹窗（基本信息+后续API支持提示） |
| 集合访问日志 | ❌ 未实现 | |
| 集合导入/导出 | ⏳ 按钮占位 | 详情页有导入/导出/模板/标签按钮 |
| 集合模板 | ⏳ 按钮占位 | |
| 集合标签/分类 | ⏳ 按钮占位 | |

### 14.9 设置页（/settings）— 全部缺口已补齐 ✅

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 个人资料 | ✅ 已实现 | |
| 偏好设置（主题/语言/密度/侧边栏） | ✅ 已实现 | 暗色/亮色/跟随系统、中文/英文、紧凑/舒适 |
| 安全设置（修改密码/2FA/设备管理/登录历史） | ✅ 已实现 | 含密码强度校验 |
| 数据管理（清除缓存/导出/导入） | ✅ 已实现 | |
| 通知偏好（站内/邮件/DND） | ✅ 已实现 | 按事件类型开关 + 免打扰时段 |
| 快捷键自定义 | ✅ 已实现 | 快捷键参考表 |
| 账户管理（注销/数据删除） | ✅ 已实现 | |
| 关于页面（版本/构建时间/许可证/健康链接） | ✅ 已实现 | |

### 14.10 全局层面

| 原缺口 | 状态 | 说明 |
|--------|------|------|
| 面包屑导航 | ❌ 未实现 | |
| 404/500 页面 | ✅ 已实现 | `error.tsx` + `not-found.tsx` |
| 操作确认统一规范 | ⏳ 部分实现 | 主要操作已有 Dialog 二次确认 |
| 表单自动保存 | ❌ 未实现 | |
| 离线提示 | ❌ 未实现 | |
| 多标签页状态同步 | ❌ 未实现 | BroadcastChannel |
| 键盘快捷键 | ✅ 已实现 | Command Palette + 问号面板 |
| 打印样式 | ✅ 已实现 | `@media print` 在 `globals.css` |
| 无障碍（a11y） | ⏳ 部分实现 | 基础 `role` 存在，系统化支持不足 |
| 加载骨架屏统一 | ✅ 已实现 | 各页面均有骨架屏加载态 |
| 空状态差异化 | ✅ 已实现 | 各页面按场景定制 EmptyState |
