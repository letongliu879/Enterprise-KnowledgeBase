[project]
name = "reality-rag-persistence"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy>=2.0",
    "reality-rag-contracts",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
postgres = [
    "psycopg2-binary>=2.9",
]