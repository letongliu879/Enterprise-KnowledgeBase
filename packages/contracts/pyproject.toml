[project]
name = "reality-rag-contracts"
version = "0.1.0"
description = "Shared contracts and schemas for Reality-RAG V2"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
]