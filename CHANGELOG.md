# Changelog

## 0.1.0 (2026-06)

- Initial release
- Document intake governance pipeline (upload → conversion → review → approval → publish → index)
- Permission-aware hybrid retrieval (BM25 + Dense Vector + Rerank)
- REST + MCP Streamable HTTP dual entry
- Projection sync for runtime data (profile, index, api key)
- Web-based workbench for document processing and approval
- Admin console for collection/profile/api-key management
