openapi: 3.1.0
info:
  title: Reality-RAG Access API
  version: 0.1.0
paths:
  /v1/retrieve:
    post:
      operationId: retrieveKnowledgeContext
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              additionalProperties: false
              properties:
                query:
                  type: string
                collection_scope:
                  type: array
                  items:
                    type: string
                filters:
                  type: object
                language:
                  type: string
                cross_languages:
                  type: array
                  items:
                    type: string
                keyword:
                  type: boolean
                meta_data_filter:
                  type: object
                retrieval_profile_id:
                  type: string
                profile:
                  type: string
                  deprecated: true
                token_budget:
                  type: integer
                debug:
                  type: string
                  enum: [none, basic, full]
              required: [query, collection_scope]
      responses:
        "200":
          description: Knowledge context
          content:
            application/json:
              schema:
                $ref: ../schemas/KnowledgeContext.schema.json
  /internal/api-key-projections/sync:
    post:
      operationId: syncApiKeyProjection
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: ../schemas/ApiKeyProjectionSync.schema.json
      responses:
        "200":
          description: Projection synced successfully
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  synced_at:
                    type: string
                    format: date-time
                  runtime_synced:
                    type: boolean
                required: [synced_at, runtime_synced]
        "400":
          description: Invalid command envelope or payload
        "409":
          description: Idempotency conflict or stale projection version
  /health:
    get:
      operationId: accessHealth
      responses:
        "200":
          description: Service health
          content:
            application/json:
              schema:
                type: object
                additionalProperties: false
                properties:
                  service:
                    type: string
                  status:
                    type: string
                  retrieval_status:
                    type: string
                required: [service, status, retrieval_status]
